@model IEnumerable<Kodimax_web.Models.TicketCandy>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_candy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.total)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Candy.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_candy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.total)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Candy.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_ticket_candy }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_ticket_candy }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_ticket_candy })
        </td>
    </tr>
}

</table>
